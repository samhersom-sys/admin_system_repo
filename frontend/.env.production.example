# First production release does not require any frontend-only environment variables.
#
# The SPA derives its API host automatically:
#   https://app.policyforge.com  ->  https://api.policyforge.com
#   http://localhost:5173        ->  /api via the local Vite proxy
#
# Add variables here only if the frontend later needs explicit build-time configuration.